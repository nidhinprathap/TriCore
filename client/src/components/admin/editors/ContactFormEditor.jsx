const ContactFormEditor = ({ data = {}, onChange }) => (
  <div className="space-y-4">
    <p className="text-xs text-tc-dim">The contact form is rendered automatically. No additional configuration needed.</p>
  </div>
);

export default ContactFormEditor;
